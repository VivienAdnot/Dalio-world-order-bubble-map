{
  "name": "dalio-world-order-bubble-map",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Dalio-style Bubble Gauge — 7 sleeves ETF géographiques Amundi PEA notés sur 6 facteurs (100 = attractif / peu de bulle), carte + radars + barres.",
  "scripts": {
    "dev": "vite --open /app.html",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && node -e \"require('fs').copyFileSync('dist/app.html','index.html')\""
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vite-plugin-singlefile": "^2.0.0"
  },
  "dependencies": {
    "echarts": "^5.5.0"
  }
}
